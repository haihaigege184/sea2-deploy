# =============================================================================
# sea2-server/.gitignore — sea2 服务端（激活服务 + 运维后台）敏感隔离清单
#
# 铁律：任何真实密钥 / 运行数据 / 本地状态 一律不入库。
# 每个条目后标注「敏感原因」，改动前先确认不会泄露生产信息。
# =============================================================================

# ---------- 运行环境变量（含真实 token / 密钥 / 管理员口令）----------
# 服务端环境变量文件：含 ADMIN_TOKEN、ALIPAY_PRIVATE_KEY 等真实密钥，
# 泄露 = 运维后台可被登录、支付宝密钥被滥用。只允许 .example 模板入库。
config.env
.env
.env.*
!.env.example
!config.env.example

# ---------- 运行期数据 / 数据库（业务数据 + 签名私钥）----------
# data/keys.json 含激活码签名私钥等运行密钥；data/ 还含订单/用户/试用业务数据，
# 泄露 = 激活体系可被伪造。运行数据只存在于部署机，绝不入库。
data/
*.db
*.sqlite
*.sqlite3

# ---------- 依赖（体积大 + 可重建，禁止入库）----------
node_modules/
node_modules_bak/

# ---------- 日志（含请求路径/机器码/订单号等敏感轨迹）----------
*.log
logs/
npm-debug.log*
yarn-error.log*

# ---------- 密钥 / 证书 / 凭据文件（防误提交）----------
*.pem
*.key
id_rsa*
*.p12
*.pfx
*.jks
server_keys.json
test_license.json
credentials.json

# ---------- 备份 / 历史垃圾（第三方越权改动遗留，禁止入库）----------
*.bak
*.bak2
*.bak.*
*.prev
*.backup
*.old
*.orig
*.rej

# ---------- Windows 保留名（git 无法正确处理；历史曾误生成）----------
nul
NUL
con
prn
aux
com1
lpt1

# ---------- 测试临时产物（不入库，但保留 test/ 源码）----------
test/tmp/
test/*.out.txt
*.tmp
coverage/
.tmp/

# ---------- 编辑器 / 系统 ----------
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/
